# UI Tasks

## Case Intake and Navigation

- `UI-001` Design case list and case detail information architecture for audit teams.
- `UI-002` Build create-case form with required metadata validation and helpful defaults.
- `UI-003` Build upload experience for IDR notices with progress, status, and failure handling.
- `UI-004` Add case timeline showing uploads, pipeline runs, approvals, and exports.

## OCR and Question Review

- `UI-005` Build document viewer with page references and extracted text traceability.
- `UI-006` Build OCR review state showing confidence issues and retry controls.
- `UI-007` Build question review workspace with edit, split, merge, add, and remove actions.

## Classification, Mapping, and Retrieval

- `UI-008` Create question detail panel for intent, risk, and ontology metadata review.
- `UI-009` Build precedent retrieval experience with ranking rationale and metadata filters.
- `UI-010` Add saved precedent area linked to each question.

## Strategy and Workflow

- `UI-011` Build structured strategy review screen with editable sections and approval controls.
- `UI-012` Build workflow graph or task board showing dependencies, owners, due dates, and blocked states.
- `UI-013` Add task detail drawer with evidence expectations and activity history.

## Evidence and Validation

- `UI-014` Build evidence repository views with search, filters, lineage, and linked objects.
- `UI-015` Build evidence upload and metadata capture form optimized for audit workflows.
- `UI-016` Build evidence validation findings panel with severity, rationale, and resolution actions.

## Draft, Review, and Package

- `UI-017` Build draft response editor or viewer with inline citations and caveat markers.
- `UI-018` Build review workflow UX for approve, reject, comment, and request-more-evidence actions.
- `UI-019` Build submission package assembly and preview screen with question coverage and attachment index.
- `UI-020` Build export history and package lineage view.

## Admin and Governance

- `UI-021` Build admin screens for prompts, templates, corpora, and thresholds.
- `UI-022` Build audit log and approval history views for compliance users.
- `UI-023` Add role-aware navigation and permission states across the application.
